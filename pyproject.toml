[project]
name = "ai-workflow-kit"
version = "0.2.0"
description = "用于文章整理与多平台成稿的模板合并工具"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "pyyaml>=6.0.2",
]

[tool.uv]
package = false
